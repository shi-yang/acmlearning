#include <bits/stdc++.h>
using namespace std;
const int N = 1005;
char s[N][N];
int n;
int main()
{
  while (~scanf("%d", &n) && n) {
    for (int i = 0; i < n; i++) {
      scanf("%s", s[i]);
    }
    for (    
  }
  return 0;
}