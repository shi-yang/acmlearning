#include <bits/stdc++.h>
using namespace std;
const int N = (int) 1e6 + 10;
int a[N];
int main()
{
	int n, m;
	while (~scanf("%d %d", &m, &n)) {
		for (int i = 0; i < n; i++) {
			scanf("%d", a + i);
		}
	}
	return 0;
}