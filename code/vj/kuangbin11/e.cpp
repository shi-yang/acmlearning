#include <iostream>
#include <algorithm>
#include <cstdio>
using namespace std;
const int maxn = 55;
int n, m, k;
int main()
{
    while (~scanf("%d %d %d", &n, &m, &k) && (n + m + k)) {
        for (int i = 1; i <= n; i++) {
            for (int j = 0; j < k; j++) {
                int x;
                scanf("%d", &x);
            }
        }
        for (int i = 1; i <= m; i++) {
            for (int j = 0; j < k; j++) {
                int x;
                scanf("%d", &x);
            }
        }
        for (int i = 0; i < k; i++) {
            for (int j = 0; j < n; j++) {
                for (int d = 0; d < m; d++) {
                    
                }
            }
        }
    }
    return 0;
}