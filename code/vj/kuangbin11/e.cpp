#include <iostream>
#include <algorithm>
#include <cstdio>
using namespace std;
const int maxn = 55;
int n, m, k;
int main()
{
  while (~scanf("%d %d %d", &n, &m, &k) && (n + m + k)) {
    for (int i = 0; i < n; i++) {
      for (int j = 0; j < k; j++) {
        
      }
    }
    for (int i = 0; i < m; i++) {
      
    }
  }
  return 0;
}