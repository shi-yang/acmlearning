#include <cstdio>
#include <iostream>
using namespace std;
const int maxn = 105;
int m[maxn];
int v[maxn];
double dp[maxn][maxn];
int main()
{
	int t;
	scanf("%d", &t);
	while (t--) {
		double p;
		int n;
		cin >> p >> n;
		for (int i = 0; i < n; i++) {
			cin >> m[i] >> v[i];
		}

	}
	return 0;
}
